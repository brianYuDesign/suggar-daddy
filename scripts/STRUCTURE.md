# Scripts 目錄結構

```
scripts/
├── README.md                           # 使用指南
├── REFACTOR_REPORT.md                  # 重構報告
├── CHECKLIST.md                        # 檢查清單
├── STRUCTURE.md                        # 本文件（目錄結構）
│
├── core/                               # 核心工具庫
│   ├── error-handler.sh               # 統一錯誤處理和日誌 (200+ 行)
│   ├── port-checker.sh                # 端口檢查工具 (130+ 行)
│   ├── wait-for-service.sh            # 智能等待服務就緒 (250+ 行)
│   └── parallel-start.sh              # 並行啟動助手 (180+ 行)
│
├── dev/                                # 開發環境管理
│   ├── start.sh                       # 啟動開發環境 (250+ 行)
│   ├── stop.sh                        # 停止服務 (80+ 行)
│   └── reset.sh                       # 重置環境 (120+ 行)
│
├── test/                               # 測試腳本
│   ├── unit.sh                        # 單元測試 (90+ 行)
│   ├── e2e.sh                         # E2E 測試 (150+ 行)
│   ├── integration.sh                 # 整合測試 (80+ 行)
│   └── coverage.sh                    # 覆蓋率報告 (90+ 行)
│
├── build/                              # 建構腳本
│   ├── build-all.sh                   # 建構所有項目 (80+ 行)
│   ├── build-backend.sh               # 建構後端服務 (120+ 行)
│   └── build-frontend.sh              # 建構前端應用 (100+ 行)
│
├── deploy/                             # 部署腳本
│   ├── deploy-dev.sh                  # 部署到開發環境 (130+ 行)
│   ├── deploy-staging.sh              # 部署到 Staging (60+ 行)
│   └── deploy-prod.sh                 # 部署到生產環境 (70+ 行)
│
├── db/                                 # 資料庫管理
│   ├── migrate.sh                     # 資料庫遷移 (190+ 行)
│   ├── seed.sh                        # 載入種子資料 (130+ 行)
│   └── backup.sh                      # 備份資料庫 (180+ 行)
│
├── legacy/                             # 舊腳本備份
│   ├── backup-database.sh
│   ├── cleanup-docs.sh
│   ├── commit.sh
│   ├── dev-start.sh
│   ├── e2e-admin-start.sh
│   ├── git-cleanup-helper.sh
│   ├── seed-redis-test-users.js
│   ├── start-e2e-env.sh
│   └── start-e2e-services.sh
│
├── migrations/                         # 資料庫遷移文件（保留）
│   ├── 001_upgrade.sql
│   ├── 001_downgrade.sql
│   ├── 002_upgrade.sql
│   └── 002_downgrade.sql
│
├── backup-database.sh                  # 保留的舊腳本（向後兼容）
├── cleanup-docs.sh                     # 保留的舊腳本
├── commit.sh                           # 保留的舊腳本
├── dev-start.sh                        # 保留的舊腳本（向後兼容）
├── e2e-admin-start.sh                  # 保留的舊腳本
├── git-cleanup-helper.sh               # 保留的舊腳本
├── db-monitoring.sql                   # SQL 腳本（保留）
├── init-db.sql                         # SQL 腳本（保留）
├── seed-redis-test-users.js            # Node.js 腳本（保留）
└── seed-test-users.sql                 # SQL 腳本（保留）
```

## 📊 統計

### 新腳本系統
- **總腳本數**: 20 個
- **核心工具**: 4 個 (~760 行)
- **開發環境**: 3 個 (~450 行)
- **測試**: 4 個 (~410 行)
- **建構**: 3 個 (~300 行)
- **部署**: 3 個 (~260 行)
- **資料庫**: 3 個 (~500 行)
- **總代碼行數**: ~2680 行

### 舊腳本（已備份）
- **備份數量**: 11 個
- **保留位置**: legacy/

### 文檔
- **README.md**: 使用指南
- **REFACTOR_REPORT.md**: 重構報告
- **CHECKLIST.md**: 檢查清單
- **STRUCTURE.md**: 目錄結構

## 🔑 關鍵特性

### 核心工具庫
所有新腳本都基於統一的核心工具庫：
1. **錯誤處理**: 統一的日誌和錯誤處理
2. **端口檢查**: 智能端口管理
3. **智能等待**: 基於健康檢查，不使用 sleep
4. **並行啟動**: 節省 30-40% 時間

### 設計原則
1. **模組化**: 可重用的工具函數
2. **統一風格**: 一致的代碼結構
3. **完整文檔**: 每個腳本都有幫助信息
4. **錯誤處理**: 清晰的錯誤消息和日誌
5. **向後兼容**: 保留舊腳本

## 🚀 快速開始

```bash
# 啟動開發環境
npm run dev

# 運行測試
npm run test:unit

# 建構項目
npm run build:all

# 資料庫遷移
npm run db:migrate

# 查看幫助
./scripts/dev/start.sh --help
```

## 📝 使用說明

詳細的使用說明請查看：
- **scripts/README.md** - 完整使用指南
- **各腳本的 --help 選項** - 特定腳本的幫助

## 🔄 遷移路徑

舊腳本 → 新腳本：
- `dev-start.sh` → `dev/start.sh` (或 `npm run dev`)
- `start-e2e-env.sh` → `test/e2e.sh` (或 `npm run test:e2e`)
- `backup-database.sh` → `db/backup.sh` (或 `npm run db:backup`)

所有舊腳本仍可使用，但建議遷移到新系統。
