# OpenClaw Workspace Governance - 實操指南 & 情境範例

## 🎯 快速開始

### 已安裝的套件
- ✅ `@adamchanadam/openclaw-workspace-governance@latest`
- ✅ 配置文件: `~/.openclaw/workspace/.govrc`
- ✅ 審計日誌目錄: `~/.openclaw/workspace/.gov_audit/`

### 配置說明

```json
{
  "mode": "managed",           // 受控模式（vs audit_only）
  "strictness": "medium",      // 中等嚴格（vs strict/loose）
  "workflows": {
    "PLAN": true,              // 強制計劃階段
    "READ": true,              // 強制閱讀現有內容
    "CHANGE": true,            // 執行變更
    "QC": true,                // 自動品質檢查
    "PERSIST": true            // 保存 + 記錄
  },
  "fastLane": {
    "enabled": true,           // 小改動快速通道
    "maxSize": "10kb",         // <10KB 的改動可豁免
    "patterns": ["MEMORY.md", "*.log", "daily notes"]
  },
  "bypass": {
    "emergency": true,         // 緊急情況可繞過
    "requireReason": true,     // 但需要記錄原因
    "logBypass": true          // 所有繞過都有日誌
  }
}
```

---

## 📚 工作流詳解

### 完整工作流 (PLAN → READ → CHANGE → QC → PERSIST)

```
情境: 需要修改 MEMORY.md 中的 Brian 偏好設置

Step 1: PLAN (計劃)
┌─────────────────────────────────┐
│ 寫出改動的目的、範圍、預期效果  │
│                                 │
│ 範例:                           │
│ Why: "記錄 Brian 新的首選項"     │
│ What: 修改 MEMORY.md 偏好段     │
│ Impact: 影響 ATC 決策邏輯       │
│ Risk: 低（純記錄）              │
└─────────────────────────────────┘
        ↓
Step 2: READ (讀)
┌─────────────────────────────────┐
│ 讀取現有檔案，理解結構          │
│                                 │
│ 檢查:                           │
│ ✓ 當前 MEMORY.md 長什麼樣       │
│ ✓ 有無其他依賴這個文件的地方    │
│ ✓ 新改動會否衝突                │
└─────────────────────────────────┘
        ↓
Step 3: CHANGE (改)
┌─────────────────────────────────┐
│ 執行實際改動                    │
│                                 │
│ 使用標準 tools:                 │
│ • read / write / edit / exec    │
│                                 │
│ 改動被記錄:                     │
│ • 時間戳                        │
│ • 改前 / 改後                   │
│ • 執行者 (Javis)                │
└─────────────────────────────────┘
        ↓
Step 4: QC (品質檢查)
┌─────────────────────────────────┐
│ 自動驗證改動的品質              │
│                                 │
│ 檢查項:                         │
│ ✓ 語法正確 (JSON/YAML/MD)      │
│ ✓ 引用完整 (沒有斷裂連結)       │
│ ✓ 依賴檢查 (改動是否會破壞其他) │
│ ✓ 風格一致 (遵循現有慣例)       │
│                                 │
│ 結果: PASS / FAIL / WARNING     │
└─────────────────────────────────┘
        ↓
    如果 FAIL → 回到 Step 3 重新改
    如果 WARNING → 人工審核決定是否繼續
    如果 PASS → 進入 Step 5
        ↓
Step 5: PERSIST (持久化)
┌─────────────────────────────────┐
│ 保存改動 + 生成完整記錄          │
│                                 │
│ 保存:                           │
│ • 修改後的檔案                  │
│ • git commit (自動)             │
│                                 │
│ 記錄:                           │
│ • Run report (.gov_audit/)     │
│ • 索引證據 (改動追蹤)           │
│ • 時間軸 (完整審計)             │
└─────────────────────────────────┘
```

### FastLane 工作流 (快速通道，小改動)

```
情況: 只是在 MEMORY.md 末尾加一行日期

因為:
  • <10KB 改動
  • 符合 FastLane 模式
  • 低風險改動

簡化流程:
  PLAN (省略) → READ (快速) → CHANGE → QC (簡化) → PERSIST

結果: 1 分鐘完成，無需詳細計劃
```

---

## 🎬 實操情境範例

### 情境 1️⃣: 修改配置文件（典型流程）

**場景**: 需要更新 `atc-config.json` 中的 P0 修復時間從 10:00 改成 09:30

#### 通常做法（無治理）
```
你: 直接編輯 atc-config.json
   ↓
改完才想起: "這會影響哪些地方？"
   ↓
可能破壞 cron job 配置
   ↓
隔天 09:30 沒有 P0 修復
   ↓
調試 1 小時才發現問題
```

#### 使用 Governance 的做法

**步驟 1: PLAN**
```
📋 你的計劃:
  Why: "Brian 要求提早 P0 修復時間到 09:30"
  What: 修改 atc-config.json 中的 p0_time
  Impact: 
    - 影響 cron job "atc-p0-auto-fix"
    - 影響 Telegram 通知時間
  Risk: 中等（需驗證 cron 配置同步）
  Verification: 改後要驗證 cron job 是否自動更新
```

**步驟 2: READ**
```
系統自動讀取:
  • 當前 atc-config.json (搜尋相關 cron job 配置)
  • 是否有其他檔案引用 "10:00"
  
發現:
  ✓ atc-config.json 中有 "10:00"
  ✓ cron job 也有 "0 10 * * *" 配置
  ✓ MEMORY.md 文檔中提到 "10:00 AM"
  
→ 這些都需要同步改動！
```

**步驟 3: CHANGE**
```
執行改動:
  1. atc-config.json: "10:00" → "09:30"
  2. Cron job: "0 10" → "0 9 30"
  3. MEMORY.md: 更新時間說明
  4. 提交 git commit
  
每次改動都被記錄:
  ✓ 改動時間: 2026-02-19 09:15:30 GMT+8
  ✓ 改動者: Javis
  ✓ 原始值 / 新值
  ✓ 原因引用: "Brian 需求"
```

**步驟 4: QC (自動品質檢查)**
```
檢查項:
  ✓ atc-config.json: 語法正確 (JSON valid)
  ✓ cron 表達式: "0 9 * * *" 有效
  ✓ 時區一致: 所有時間都用 Asia/Taipei
  ✓ 引用完整: MEMORY.md 中的時間已更新
  ✓ 依賴檢查: 沒有其他檔案需要同步
  
結果: ✅ PASS (所有檢查通過)
```

**步驟 5: PERSIST**
```
✅ 改動已保存
   • 檔案已更新
   • git 已提交 (message: "fix: Change P0 auto-fix time to 09:30")

📊 生成報告 (.gov_audit/2026-02-19_093015.json):
{
  "type": "config_update",
  "timestamp": "2026-02-19T09:30:15Z",
  "executor": "Javis",
  "reason": "Brian 需求",
  "changes": [
    {
      "file": "atc-config.json",
      "field": "p0_time",
      "before": "10:00",
      "after": "09:30"
    },
    {
      "file": "cron job atc-p0-auto-fix",
      "field": "schedule.expr",
      "before": "0 10 * * *",
      "after": "0 9 * * *"
    },
    {
      "file": "MEMORY.md",
      "field": "automation.p0_auto_fix.time",
      "before": "10:00",
      "after": "09:30"
    }
  ],
  "qc_result": "PASS",
  "git_commit": "a1b2c3d",
  "impact_areas": ["automation", "notification_timing"]
}

📍 索引證據:
   • 如果 30 天後想回顧「為什麼改了時間？」
   • 只需查 .gov_audit/2026-02-19_093015.json
   • 立即看到完整原因 + 影響範圍
```

**效果對比**:
```
無治理: 改完後 2 小時發現 cron 沒同步
       浪費時間調試 → 後悔為什麼沒備份

有治理: 改動前 QC 就發現「還需改 cron job」
       自動同步所有相關檔案
       完整記錄為什麼改 + 改了什麼
```

---

### 情境 2️⃣: 緊急修復（P0 問題）

**場景**: 凌晨 2 點發現 P0 bug，需要立即修復，但不想經過完整工作流

#### 快速應急流程

**通常做法**（無治理）:
```
Bug 發現 → 直接改代碼 → 沒有任何記錄
   ↓
改完才想起: "誰改的？為什麼改？"
   ↓
隔天早上 Brian 問: "這個改動哪裡來的？"
   ↓
查不出來 → 信任危機
```

**使用 Governance 的做法**:

**使用 Emergency Bypass**
```
/gov_bypass "CRITICAL: Payment API 返回 500
            Temporary fix: 回滾到上個版本
            Emergency ticket: #P0-2026-02-19
            Duration: 臨時修復，早上 10 點前需要根本修復"

系統:
  ✓ 允許 bypass PLAN/READ/QC 階段
  ✓ 跳直到 CHANGE
  ✓ 但仍然自動記錄所有改動
  ✓ 標記為「BYPASS」+ 原因
```

**結果**:
```
✅ 快速完成緊急修復（節省 5-10 分鐘）
✅ 但完整記錄被保存到 .gov_audit/
   {
     "type": "emergency_bypass",
     "timestamp": "2026-02-19T02:15:30Z",
     "reason": "Payment API 500 error",
     "bypass_reason": "CRITICAL",
     "changes": [...],
     "requires_review": true,
     "followup_ticket": "#P0-2026-02-19"
   }

⚠️ 早上 10 點 Brian 醒來：
   看到 .gov_audit/ 通知
   → "凌晨有 emergency bypass 發生"
   → 查詳情 → 了解發生了什麼
   → 安排根本修復

效果: 應急快，但透明 + 可追溯
```

---

### 情境 3️⃣: FastLane 小改動

**場景**: 只是在 MEMORY.md 底部加一行「2026-02-19 已啟用 governance」

**標準流程** (5 步驟，3-5 分鐘):
```
PLAN → READ → CHANGE → QC → PERSIST
```

**FastLane 流程** (簡化版，30 秒):
```
改動偵測:
  • 檔案: MEMORY.md (符合 FastLane 模式)
  • 大小: <1KB (符合 10KB 限制)
  • 風險: 低

✅ 自動啟用 FastLane:
  • PLAN: 跳過（太簡單）
  • READ: 快速掃描（檢查基本結構）
  • CHANGE: 立即執行
  • QC: 簡化版（只檢查 Markdown 語法）
  • PERSIST: 快速保存 + 記錄

時間: 30 秒完成 vs 3-5 分鐘

審計日誌仍然保存:
{
  "type": "fastlane_edit",
  "file": "MEMORY.md",
  "timestamp": "2026-02-19T08:59:00Z",
  "size_bytes": 245,
  "qc_time_ms": 50,
  "fastlane": true
}
```

---

### 情境 4️⃣: 多檔案修改與依賴檢查

**場景**: 新增一個新的 Cron Job，需要同步更新 5 個檔案

```
任務: 新增每天 14:00 的「性能報告」cron job

需要改動:
  1. atc-config.json → 新增 cron job 定義
  2. SKILL.md → 文檔中說明新 cron job
  3. MEMORY.md → 記錄新功能
  4. 實際 cron job → 建立任務
  5. Telegram channel → 配置通知目標

無治理情況:
  改 1 → 忘記改 2 → 隔天發現文檔錯了
       → 改 2 → 忘記改 3 → MEMORY 沒更新
       → 改 3 → 忘記改 4 → cron job 沒建
       → 改 4 → 忘記改 5 → 通知不工作

總共浪費 2 小時除錯
```

**使用 Governance**:

**Step 1: PLAN**
```
定義改動範圍:
  Adding: Performance report cron job (14:00 daily)
  Scope:
    - Config definition
    - Documentation
    - Memory record
    - Job creation
    - Notification setup
  Estimated impact: Medium (5 files)
  Dependencies: 無其他 cron job 衝突
```

**Step 2: READ**
```
系統自動掃描:
  ✓ atc-config.json: 查現有 cron job 結構
  ✓ SKILL.md: 查文檔格式
  ✓ MEMORY.md: 查記錄位置
  ✓ Cron: 查現有 job，尋找 14:00 衝突
  
結果: 
  ⚠️ 14:00 暫時無衝突
  ⚠️ 但 14:05 有 "metrics-collection" job
      → 考慮間隔以避免重疊
```

**Step 3: CHANGE**
```
同時修改 5 個檔案:
  1. atc-config.json: +performance_report_job
  2. SKILL.md: + 文檔
  3. MEMORY.md: + 記錄
  4. Cron: 建立任務
  5. Telegram routing: 配置通知

所有改動被記錄成一個「transaction」
```

**Step 4: QC**
```
✅ 自動依賴檢查:
   • atc-config.json: JSON valid
   • SKILL.md: 檔案引用正確
   • MEMORY.md: 格式一致
   • Cron job: 調度表達式有效
   • Telegram: 群組 ID 存在
   
⚠️ 發現潛在問題:
   「14:05 有 metrics-collection，改 14:10？」

💡 自動建議:
   為避免重疊，建議將效能報告調整到 14:30
```

**Step 5: PERSIST**
```
改動已完全同步:
  ✓ 所有 5 個檔案更新
  ✓ Cron job 已建立
  ✓ 通知已配置

📊 生成完整報告:
{
  "type": "multi_file_transaction",
  "files_modified": 5,
  "timestamp": "2026-02-19T09:45:00Z",
  "changes": [
    { "file": "atc-config.json", "type": "add", "path": "$.cron_jobs.performance_report" },
    { "file": "SKILL.md", "type": "add", "line": 425 },
    { "file": "MEMORY.md", "type": "add", "line": 89 },
    { "file": "cron", "type": "create", "job_id": "perf-report-14-30" },
    { "file": "telegram_routing.json", "type": "add", "channel": "g-sa-specs" }
  ],
  "dependencies_checked": true,
  "qc_warnings": 1,
  "resolved": true
}
```

**效果**:
```
無治理: 改完 1 天後發現有 2 個檔案漏改
       花 30 分鐘追蹤哪裡出問題
       
有治理: 改完立即看到完整清單
       自動提醒「還有 3 個檔案需要改」
       自動偵測相關引用是否都更新
```

---

## 🎮 實際命令速查

### 查看審計日誌

```bash
# 看最近改動
cat ~/.openclaw/workspace/.gov_audit/latest.json | jq

# 查某天的改動
ls ~/.openclaw/workspace/.gov_audit/ | grep 2026-02-19

# 查某次改動的完整細節
jq '.changes[] | select(.file == "MEMORY.md")' ~/.openclaw/workspace/.gov_audit/*
```

### 查看改動歷史

```bash
# 看改動時間線
jq '.[] | {timestamp, executor, type}' ~/.openclaw/workspace/.gov_audit/* | head -20

# 看誰改了什麼
jq '.[] | select(.executor == "Javis")' ~/.openclaw/workspace/.gov_audit/*

# 看改動原因
jq '.[] | {timestamp, reason, impact_areas}' ~/.openclaw/workspace/.gov_audit/*
```

### 緊急 Bypass

```bash
# 緊急情況下快速改動
/gov_bypass "CRITICAL: [原因] [改動內容] [預計何時修復]"

# 例子:
/gov_bypass "CRITICAL: Auth service down
            Temp fix: 關閉 2FA
            Permanent fix: ETA 10:00 AM"
```

### 強制重新掃描

```bash
# 如果懷疑 QC 遺漏了什麼
/gov_rescan ~/Project/suggar-daddy

# 看完整掃描報告
cat ~/.openclaw/workspace/.gov_audit/rescan_latest.json | jq
```

---

## 📍 何時使用哪種流程

| 情況 | 流程 | 時間 | 例子 |
|------|------|------|------|
| **普通改動** | 完整 (5 步) | 3-5 分鐘 | 更新配置、新增功能 |
| **小改動** | FastLane | 30 秒 | 加一行日期、修正typo |
| **緊急修復** | Bypass | 1 分鐘 | P0 bug，凌晨 2 點 |
| **複雜改動** | 完整 + 手動 | 10-20 分鐘 | 多檔案依賴改動 |

---

## ✅ 檢查清單

引入後第 1 天要做的:

- [ ] 讀懂工作流 5 步驟
- [ ] 試做一個小改動 (FastLane)
- [ ] 查看審計日誌 (*.json 格式)
- [ ] 做一個中等改動 (完整流程)
- [ ] 試過 1 次 bypass 流程
- [ ] 設置 QC 偏好 (.govrc 調整)

---

_部署完成 | 2026-02-19 09:00 GMT+8_
