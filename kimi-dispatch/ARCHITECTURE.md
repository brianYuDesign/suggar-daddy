# Kimi Dispatch 架構文檔

## 📐 系統架構

```
┌─────────────────┐
│   Javis (Main)  │
└────────┬────────┘
         │ "用戶要求派發 Kimi 任務"
         ↓
┌─────────────────────────────────┐
│  Javis Decision Layer           │
│  - 檢測 "kimi" 觸發詞            │
│  - 提取任務描述                  │
│  - 調用 dispatch-kimi.sh        │
└────────┬────────────────────────┘
         │ exec()
         ↓
┌─────────────────────────────────────────┐
│  dispatch-kimi.sh (快速派發)              │
│  - 驗證 API Key                         │
│  - 生成 Task ID                        │
│  - 構建 API 請求體                      │
│  - 調用 Kimi API（非阻塞 ✅）            │
│  - 保存待派發狀態                       │
│  - 立即返回                             │
└────────┬────────────────────────────────┘
         │ curl POST /chat/completions
         ↓
┌──────────────────────────────────────────────┐
│  Kimi API (雲端)                             │
│  https://api.moonshot.cn/v1                  │
│  - 模型：moonshot-v1-128k                    │
│  - 完全異步處理                             │
│  - 支援 Webhook 回調                         │
└────────┬─────────────────────────────────────┘
         │ 立即返回 API ID
         │ 後台 30-120 秒處理
         ↓ 處理完成後 → HTTP POST Webhook
┌──────────────────────────────────────────────┐
│  kimi-webhook.js (Node.js Express 伺服器)    │
│  Port 9001 - 監聽 /kimi/webhook/:taskId      │
│  - 驗證 Webhook 簽名                        │
│  - 解析 Kimi 完成響應                       │
│  - 提取結果 & Token 消耗                    │
│  - 保存到 results/latest.json              │
│  - 調用 /api/cron/wake 喚醒 Javis          │
└────────┬───────────────────────────────────┘
         │ curl POST /api/cron/wake
         ↓
┌──────────────────────────────┐
│  OpenClaw Gateway            │
│  - 喚醒主 session            │
│  - 發送 system event         │
└────────┬─────────────────────┘
         │ session.inject("Kimi 任務完成：...")
         ↓
┌─────────────────────────────────┐
│  Javis Heartbeat / main()       │
│  - 檢查 results/latest.json    │
│  - 發現新完成的任務             │
│  - 生成結果摘要                 │
│  - 回復給用戶                   │
└─────────────────────────────────┘
         │
         ↓ reply()
    ✅ 完成！
```

---

## 🔄 數據流

### 1. 派發階段（Dispatch Phase）

```
用戶消息
  ↓
{
  "text": "Kimi：寫一個 Python 計算器，包含測試"
}
  ↓
Javis 解析
  ├─ 檢測到 "Kimi:" 前綴
  ├─ 提取：「寫一個 Python 計算器，包含測試」
  └─ 生成任務名稱：「calculator」
  ↓
exec(./dispatch-kimi.sh -p "..." -n "calculator")
  ↓
dispatch-kimi.sh 執行
  ├─ 驗證 KIMI_API_KEY
  ├─ 生成 TASK_ID: kimi-1707654321-12345
  ├─ 構建請求體：
  │  {
  │    "model": "moonshot-v1-128k",
  │    "messages": [...],
  │    "temperature": 0.3,
  │    "max_tokens": 8000
  │  }
  ├─ 調用 API（POST /chat/completions）
  │  ↓ curl -s -X POST $KIMI_API_URL/chat/completions \
  │     -H "Authorization: Bearer $KIMI_API_KEY"
  │     -d "$API_PAYLOAD"
  ├─ 保存待派發狀態：
  │  ~/.openclaw/workspace/kimi-dispatch/results/pending-kimi-1707654321-12345.json
  │  {
  │    "task_id": "kimi-1707654321-12345",
  │    "status": "dispatched",
  │    "created_at": "2026-02-17T15:30:00Z"
  │  }
  └─ 立即返回 (無阻塞)
  ↓
Javis 回覆用戶
  {
    "text": "✅ Kimi 任務已派發：calculator\n完成後會自動通知你！"
  }
```

### 2. 處理階段（Processing Phase）

```
Kimi 後台處理（30-120 秒）
  ├─ 接收提示詞
  ├─ 生成 Python 計算器代碼
  ├─ 包含測試用例
  └─ 總共使用 ~1500 tokens

完成後，Kimi 發送 HTTP POST 回調：

POST http://localhost:9001/kimi/webhook/kimi-1707654321-12345

{
  "id": "cmpl-1707654360-abc123",
  "model": "moonshot-v1-128k",
  "usage": {
    "prompt_tokens": 200,
    "completion_tokens": 1300,
    "total_tokens": 1500
  },
  "choices": [{
    "message": {
      "content": "以下是 Python 計算器實現：\n\n```python\nclass Calculator:\n    def add(a, b):\n        return a + b\n    ...\n```\n\n包含單元測試：\n```python\nimport unittest\nclass TestCalculator:\n    ...\n```"
    }
  }],
  "metadata": {
    "task_id": "kimi-1707654321-12345",
    "task_name": "calculator"
  }
}
```

### 3. 回調階段（Webhook Phase）

```
kimi-webhook.js 收到 POST /kimi/webhook/kimi-1707654321-12345

┌─ 驗證簽名 ✅
├─ 解析 Kimi 響應
├─ 提取內容 & Token 消耗
├─ 生成結果物件：
│  {
│    "task_id": "kimi-1707654321-12345",
│    "task_name": "calculator",
│    "status": "done",
│    "model": "moonshot-v1-128k",
│    "output": "Python 計算器代碼...",
│    "tokens": {
│      "prompt": 200,
│      "completion": 1300,
│      "total": 1500
│    },
│    "completed_at": "2026-02-17T15:30:30Z"
│  }
├─ 保存到 ~/.openclaw/workspace/kimi-dispatch/results/latest.json
├─ 也保存到 ~/.openclaw/workspace/kimi-dispatch/results/kimi-1707654321-12345.json
├─ 清理待派發檔案：
│  rm ~/.openclaw/workspace/kimi-dispatch/results/pending-kimi-1707654321-12345.json
└─ 喚醒 Javis：
   curl -X POST http://localhost:8888/api/cron/wake \
     -d '{"text":"Kimi 任務完成：calculator", "mode":"now"}'
```

### 4. 結果處理階段（Reply Phase）

```
Javis 被喚醒（通過 /api/cron/wake）

┌─ Heartbeat 觸發
├─ 讀取 results/latest.json
├─ 檢查 status == "done" && !acknowledged
├─ 生成摘要：
│  {
│    "task_name": "calculator",
│    "model": "moonshot-v1-128k",
│    "tokens": 1500,
│    "summary": "Python 計算器代碼...[摘要前 300 字]"
│  }
├─ 構建回複訊息：
│  ```
│  ✅ Kimi 任務完成！
│  
│  📋 任務：calculator
│  🤖 模型：moonshot-v1-128k
│  💾 Token：200 (提示) + 1300 (生成) = 1500
│  ⏱️ 完成時間：2026-02-17T15:30:30Z
│  
│  📝 結果摘要：
│  Python 計算器代碼實現，包含加減乘除四則運算...
│  
│  💡 完整結果：~/.openclaw/workspace/kimi-dispatch/results/latest.json
│  ```
├─ 發送回複給用戶
└─ 標記結果為 "acknowledged"
```

---

## 📊 Token 消耗分析

### 完整任務的 Token 流向

```
派發階段（Dispatch）
└─ exec() 調用 dispatch-kimi.sh
   └─ curl POST /chat/completions
      └─ Token 消耗：0（只是 HTTP 請求，不經過 Javis）

Kimi API 處理（Processing）
└─ Kimi 模型處理
   ├─ 輸入：200 tokens（用戶提示詞）
   └─ 輸出：1300 tokens（生成內容）
      ↓ 合計：**1500 tokens**（客戶付費）

回調階段（Webhook）
└─ HTTP POST 到本地 Webhook
   └─ Token 消耗：0（本地 HTTP，無 API 調用）

結果處理階段（Reply）
└─ Javis 讀取本地檔案
   └─ Token 消耗：0（讀檔案）

**整個流程總 Token 消耗：1500**
└─ 只在「調用 Kimi API」時計費
└─ 派發、回調、結果處理都是 0 成本
```

### 對比傳統輪詢方案

```
派發階段
└─ 調用 Kimi API
   └─ Token：1500（相同）

輪詢階段（每 5 秒檢查一次）
├─ 第 1 次檢查：「任務完成了嗎？」
│  └─ Token：50
├─ 第 2 次檢查：「任務完成了嗎？」
│  └─ Token：50
├─ ...（總共 15-20 次）
└─ 合計輪詢消耗：**1000+ tokens**

**完整流程總消耗：2500+ tokens**
**浪費：1000+ tokens（66% 浪費）**
```

---

## 🔒 安全考慮

### 1. API Key 保護

```bash
# ✅ 正確做法：環境變量
export KIMI_API_KEY="sk-xxx"

# ❌ 錯誤做法：硬編碼在腳本
./dispatch-kimi.sh -k "sk-xxx"
```

### 2. Webhook 簽名驗證

```javascript
// kimi-webhook.js 中
function verifySignature(req) {
  const signature = req.headers['x-kimi-signature'];
  const payload = JSON.stringify(req.body);
  const hash = crypto
    .createHmac('sha256', WEBHOOK_SECRET)
    .update(payload)
    .digest('hex');
  
  return signature === hash;  // ✅ 驗證完整性
}
```

### 3. 速率限制

```javascript
// 防止濫用
const rateLimiter = {
  maxTasksPerMinute: 10,
  maxConcurrentTasks: 5,
  taskTimeoutMs: 120000  // 2 分鐘超時
};
```

---

## 🧪 測試矩陣

### 單元測試

| 組件 | 測試項 | 預期結果 |
|------|-------|--------|
| dispatch-kimi.sh | API Key 驗證 | 無 key 時報錯 ✅ |
| | 參數驗證 | 缺少 prompt 時報錯 ✅ |
| | API 調用 | 返回 API ID ✅ |
| kimi-webhook.js | 簽名驗證 | 無效簽名被拒 ✅ |
| | 結果保存 | 檔案已寫入 ✅ |
| | 喚醒通知 | Gateway 被喚醒 ✅ |

### 集成測試

```bash
# 完整流程測試
./run-kimi.sh "寫一個 Python 費波那契函數"
  ↓
wait 30s
  ↓
cat results/latest.json | jq .status
# 應該返回 "done"
```

---

## 📈 性能指標

### 延遲（Latency）

```
派發 → API 返回：< 1 秒 ✅
Kimi 處理中：30-120 秒（取決於任務複雜度）
Webhook 回調 → 結果保存：< 100ms ✅
結果保存 → Javis 回複：< 500ms ✅

**端對端延遲：30-120 秒**（取決於 Kimi API 處理時間）
```

### 吞吐量（Throughput）

```
單個 Webhook 伺服器（Node.js）：
- 並發連接：100+
- QPS：1000+（完全不是瓶頸）

實際瓶頸：Kimi API 速率限制
- 默認：10 tasks/minute
- 高級帳戶：可協商更高限制
```

### 成本節省

```
每月假設：30 個任務，每個 1500 tokens

傳統輪詢方案：
  API 調用：30 × 1500 = 45,000 tokens
  輪詢浪費：30 × 1000 = 30,000 tokens
  ────────────────────────────
  總計：75,000 tokens = ¥45

Kimi Dispatch 方案：
  API 調用：30 × 1500 = 45,000 tokens
  派發成本：0 tokens ✅
  回調成本：0 tokens ✅
  ────────────────────────────
  總計：45,000 tokens = ¥27

**每月節省：¥18（40% 降低成本）**
```

---

## 🚀 擴展性

### 支援多個 Webhook 伺服器

```
用於負載均衡：

┌──────────┐   ┌──────────┐
│  Webhook │   │  Webhook │
│   Port   │───│   Port   │
│   9001   │   │   9002   │
└──────────┘   └──────────┘
     ↑              ↑
     └──────────────┘
          ↑
   Kimi API（輪詢分配）
```

### 支援多種 Kimi 模型

```bash
# 根據任務複雜度選擇模型

# 簡單任務：快速且便宜
KIMI_MODEL="moonshot-v1-8k" ./run-kimi.sh "..."

# 中等任務：平衡性能和成本
KIMI_MODEL="moonshot-v1-32k" ./run-kimi.sh "..."

# 複雜任務：最強性能
KIMI_MODEL="moonshot-v1-128k" ./run-kimi.sh "..."

# 超長文本：最新模型
KIMI_MODEL="kimi-k2.5" ./run-kimi.sh "..."
```

---

## 📝 參考資源

- **Kimi API 文檔**：https://platform.moonshot.cn/docs
- **Webhook 指南**：https://webhook.guide/
- **OpenClaw Gateway**：https://docs.openclaw.ai

---

_架構設計遵循「簡單即美」原則：最少的組件，最大的效率。_
